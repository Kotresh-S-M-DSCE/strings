class Solution {
    public int countAsterisks(String s) {
        int sum=0,count=0;
        for(int i=0;i<s.length();i++)
        {
            if(count%2==0&&s.charAt(i)=='*')
               sum++;
            if(s.charAt(i)=='|')
               count++;
        }
        return sum;
    }
}